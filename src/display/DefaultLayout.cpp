#include "display/DefaultLayout.h"

Position DefaultLayout::getNamePos() const {
    return Position{0, 0};
}

Position DefaultLayout::getTelegramPos() const {
    return Position{0, 0};
}

Position DefaultLayout::getLinesPos() const {
    return Position{0, 0};
}

Position DefaultLayout::getImagePos() const {
    return Position{0, 0};
}
